import { createClient } from @supabase/supabase-js; import fs from fs; const env = fs.readFileSync(.env.local, utf8).split(\n).reduce((acc, line) => { const [k, ...v] = line.split(=); if (k) acc[k] = v.join(=).trim(); return acc; }, {}); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function test() { const { data, error } = await supabase.from(local_db_store).select(*); console.log(READ:, data?.length, error); const { error: err } = await supabase.from(local_db_store).upsert([{key: test, data: {}}]); console.log(WRITE:, err); } test();